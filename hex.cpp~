#include <stdio.h>
#include <stdlib.h>

int main(){
    char number[20];
    char rubbish[0x100];
        printf("hexadcimal en minúsculas: ");
    scanf(" %[0-9a-fA-F]",number);
    scanf(" %[^\n]",rubbish);
    printf(" %",number);
   printf(" %",rubbish); 
    return EXIT_SUCCESS;
}
